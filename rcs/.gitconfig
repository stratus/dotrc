[init]
    defaultBranch = main

[user]
	name = Gustavo Franco
	email = stratus@acm.org
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
